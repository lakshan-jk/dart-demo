void main() {
  final onPressed = () {
    return "Enada pana pora?????";
  };
  print(onPressed());

  final manifest = ["private jet 🛩️", "Mini Cooper🚗", "Income tax raid 💰"];
  final maniFest = manifest.map((e) => e.toUpperCase());
  print(maniFest);
}
